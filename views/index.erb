<!DOCTYPE html>
<html>
  <head><link rel="stylesheet" href="index.css">
    <title>Holidays</title>
  </head>
  <body>
      <h1>Holidays on...</h1>

        <form class="form">
          <label for="month">Month:</label>
          <input name="month" type="number" min="1" max="12" value="<%= params["month"] %>" required>

          <label for="day">Day:</label>
          <input name="day" type="number" min="1" max="31" value="<%= params["day"] %>" required>

          <label for="year">Year:</label>
          <input name="year" type="number" min="1900" max="<%= Time.now.year %>" value="<%= params["year"] %>" required>

          <button type="submit">Generate!</button>
      </form><br>

        <table align="center">
            <tbody>
                <% @holidays.each do |holiday| %>
                    <tr>
                        <td>
                            <%=holiday["name"]%>
                            <%=holiday["date"]%>
                        </td>
                    </tr>
                <% end %>
            </tbody>
        </table>
  </body>
</html>
